<UserControl x:Class="Calendar_Converter.View.Week"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" Height="128" Width="472">
    <Grid>
        <Border BorderBrush="Black" BorderThickness="1" Background="{Binding WeekBackground}">
        <ItemsControl ItemsSource="{Binding DaysViewModels}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <UniformGrid Columns="7" Rows="1"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
        </ItemsControl>
        </Border>
    </Grid>
</UserControl>
