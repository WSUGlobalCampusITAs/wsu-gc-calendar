<Window
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	x:Class="Calendar_Converter.winSettings"
	x:Name="Settings"
	Title="Settings"
	Width="640" Height="480" ResizeMode="CanMinimize">
	<Window.Background>
        <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
            <GradientStop Color="Black" Offset="0"/>
            <GradientStop Color="#FF9E9E9E" Offset="0.707"/>
        </LinearGradientBrush>
    </Window.Background>

	<Grid x:Name="LayoutRoot">
		<Grid.RowDefinitions>
			<RowDefinition Height="41*"/>
			<RowDefinition Height="372*"/>
			<RowDefinition Height="38*"/>
		</Grid.RowDefinitions>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="2*"/>
			<ColumnDefinition Width="5*"/>
		</Grid.ColumnDefinitions>
		<TextBlock Text="User Settings" Foreground="White" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="16" />
		<TreeView x:Name="tvSettingMenu" Margin="0,0,0,2" Background="LightGray" Grid.Row="1" Grid.RowSpan="2"/>
		<Frame Grid.RowSpan="2" Grid.Column="1" x:Name="frmSettings"/>
		<Button Grid.Row="2" Width="100" Grid.Column="1" HorizontalAlignment="Left" Margin="10,2,0,2" Content="OK" Click="OK_Button_Click"/>
		<Button Grid.Row="2" Width="100" HorizontalAlignment="Right" Margin="0,2,10,2" Content="Cancel" Grid.Column="1" Click="Cancel_Button_Click"/>
	</Grid>
</Window>